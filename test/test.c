#include <stdio.h>
#include <stdlib.h>

int main() {
    
    
    system("pause");
    return 0;
   
}


/*

17. 用递归法将一个整数n转换成字符串。例如，输入483，应输出字符串"483"。
n的位数不确定，可以是任意位数的整数。



*/

/*结构体加系统类型  待定做不做

14. 输入10个学生5门课的成绩，分别用函数实现下列功能：
① 计算每个学生的平均分；
② 计算每门课的平均分；
③ 找出所有50个分数中最高的分数所对应的学生和课程；
④ 计算平均分方差：
σ = \frac{1}{n}\sum x_i^2 - \left(\frac{\sum x_i}{n}\right)^2
其中，x_i为某一学生的平均分。

15. 写几个函数：
① 输入10个职工的姓名和职工号；
② 按职工号由小到大顺序排序，姓名顺序也随之调整；
③ 要求输入一个职工号，用折半查找法找出该职工的姓名，从主函数输入要查找的职工号，输出该职工姓名。

*/